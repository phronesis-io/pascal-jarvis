# Config (personal)
jarvis.yaml
sources.yaml
.claude/

# Runtime state (bot + heartbeat + session tracking)
active_sessions.json
heartbeat_state.json
heartbeat_state.lock
interval_overrides.json
interval_overrides_meta.json
night_queue.jsonl
.delivery_state.json
.batch_last_flush
perception_state.json
jarvis.log
jarvis.log.[0-9]
.memory_cache
.bot.pid
.daemon.pid
.dashboard.pid
.free_time_nudge_state
.heartbeat_last_source
calendar_event_mapping.json
.harness_evolve_state
/.urgent_send

# Owner-private working memory and recovered outgoing drafts
/drafts/
/todos.md
/warm/

# Temp / backup / corrupted artifacts from atomic writes or debugging
*.bak
*.tmp
*.corrupted
*.gone
*.TEST_GONE
.session_lock_*
.dispatch_*

# EigenFlux plugin data (tokens, feed, messages, state)
# the whole runtime-state dir (anchored: plugins/eigenflux stays tracked) —
# individual entries below kept for older checkouts that track siblings
/eigenflux/
eigenflux/credentials.json
eigenflux/credentials.json.tmp
eigenflux/seen_items.json
eigenflux/seen_items.json.tmp
eigenflux/feed_store.jsonl
eigenflux/message_store.jsonl
eigenflux/publish_state.json
eigenflux/publish_state.json.tmp
eigenflux/impression_id.txt
eigenflux/user_settings.json
eigenflux/preinstall_state.json
eigenflux/.cli_upgrade_result
eigenflux/preinstall_cli_upgrade.log
eigenflux/parity_todo.md
eigenflux/.profile_refresh_state

# Python
__pycache__/
*.pyc
.env
.env.*
*.pem
*.key
*.p12
*.pfx
.pytest_cache/

# OS
.DS_Store
*.swp
*.swo

# Claude Code sessions (.jsonl in project dir)
*.jsonl

# Memory (personal data) — root-anchored so it doesn't swallow examples/memory/
# Catches both a real dir and a symlink at the repo root.
/memory
/memory/
checkin_log.jsonl
eigenflux/.eigenflux/

# Daemon & watchdog logs
daemon.log
watchdog.log

# Runtime data
engagement_log.jsonl
heartbeat_outbox.jsonl
# sched_events rotation family: *.jsonl covers the base file only; the
# rotated .1–.7 archives (core/sched_events.py GENERATIONS=7) and the flock
# sidecar match nothing without these. .1 is read back for replay — never clean.
sched_events.jsonl.*
*.jsonl.lock
tmp/
jobs/
secrets/
eigenflux/.feed_poll_state
eigenflux/.feed_backlog_flushed
eigenflux/needs_research.jsonl

# Task state files
tasks/.phronesis_last_ts
/.phronesis_last_ts

# Session backups
session_backups/

# Session compaction artifacts (per-conv-key markdown summaries)
session_compacts/

# Dashboard runtime: SQLite db + WAL, NiceGUI log, generated richview pages
/data/
/dashboard.log
/views/
/mobile_access.json

# Dashboard Python bytecode (covered by __pycache__/ above, listed for clarity)
dashboard/__pycache__/
eigenflux/pending_publish/
*.json.lock

# v2 runtime sidecars (REQ-30/31/34/39/41/42)
.last_backup_ok
.diag_last_pre.txt
.diag_last_alert.json
.deploying
.admin_token
data/.intention_inflight.json
data/.intent_breach_queue.jsonl
data/.intent_card_ledger.jsonl
.repos_sync_product.txt
.repos_sync_consumed
eigenflux/.ef-cursor
eigenflux/.ef-seen
alerts_deadletter.jsonl
.daemon_brain_state.json
.daemon_restart_state.json

# Virtualenvs (setup instructions suggest an in-repo .venv)
.venv/
venv/
memorials.jsonl.bak-*
.memorials_rotated.json
mail/.pending_batch.json
mail/.*.json
.config/taskline/
