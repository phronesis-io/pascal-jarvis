# Python runtime dependencies for Jarvis harness.
# Install with:
#   ./setup.sh
# setup uses the selected interpreter and creates ~/.jarvis/runtime-venv when
# the system Python is externally managed. For runtime-only environments:
#   python3 -m pip install -r requirements.txt

# YAML config parsing
PyYAML>=6.0

# Dashboard (NiceGUI-powered agent backend)
nicegui==3.15.0
pywebpush>=2.0
qrcode[pil]>=8.0

# Lark long-connection sidecar (card callbacks and read receipts)
lark-oapi>=1.6.0

# Testing (optional — only needed for `pytest tests/`)
# Install separately if you want to run the test suite:
#   pip install pytest
